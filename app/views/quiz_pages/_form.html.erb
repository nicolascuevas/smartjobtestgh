<%= form_with(model: [@quiz, quiz_page]) do |form| %>
  <% if quiz_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quiz_page.errors.count, "error") %> prohibited this quiz_page from being saved:</h2>

      <ul>
        <% quiz_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :quiz_id %>
    <%= form.text_field :quiz_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
